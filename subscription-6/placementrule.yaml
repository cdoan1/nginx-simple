---
apiVersion: apps.open-cluster-management.io/v1
kind: PlacementRule
metadata:
  name: nginx-simple-6
  namespace: nginx-simple-6
  labels:
    app: nginx-simple
spec:
  clusterSelector:
    matchLabels:
      name: south-east
