---
apiVersion: apps.open-cluster-management.io/v1
kind: PlacementRule
metadata:
  name: nginx-simple
  namespace: nginx-simple
  labels:
    app: nginx-simple
spec:
  clusterSelector:
    matchLabels:
      name: south-east
