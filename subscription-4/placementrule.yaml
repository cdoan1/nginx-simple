---
apiVersion: apps.open-cluster-management.io/v1
kind: PlacementRule
metadata:
  name: nginx-simple-4
  namespace: nginx-simple-4
  labels:
    app: nginx-simple
spec:
  clusterSelector:
    matchLabels:
      name: south-east
