---
apiVersion: apps.open-cluster-management.io/v1
kind: PlacementRule
metadata:
  name: nginx-simple-3
  namespace: nginx-simple-3
  labels:
    app: nginx-simple
spec:
  clusterSelector:
    matchLabels:
      name: south-east
